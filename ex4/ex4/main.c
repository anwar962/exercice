#include <stdio.h>
#include <stdlib.h>

int main()
char nom;

int T;
int max,min;
int i,somme;
float M;
printf("vuiellez saisi le nom:");
scanf("%s",&nom);
for(i=0;i<5;i++)
    printf(entrez les 5 note);
    scanf("%d",&T[i]);
    while(T[i]<0 && T[i]>0)
        printf("entrez une note valide");
    i++}
    s=0
    for (i=0;i<5;i++)
        s=s+i;
        printf("la somme est:%d",somme);
        M=s/5;
        printf("la moyenne est:%.2f",M);
        for(i=0;i<5;i++)

        if(max=T[0]){
            max=T[0];
            else(max>T[0])
            max=T[i];}
            if(min>T[0]){
            min=T[0;]
            else
            min=T[i];}
            if(M>=50)
                printf("bravo!vous etes ruessi");
            else("vous etes pas ruessi");






    return 0;
}
